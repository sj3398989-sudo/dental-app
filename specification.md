
Gemini との会話
新しいチャットへスムーズに開発環境を引き継ぐための「仕様サマリー（設計書）」と「最新の app.py 完全版コード」をまとめました。

この内容をそのまま新しいチャットの冒頭に貼り付けることで、AIがこれまでの実装状況・背景・技術構成を完全に把握した状態で続きの作業を開始できます。

🦷 AI品質管理カルテ（大阪センター）システム仕様書 & 引継ぎ資料
1. プロジェクト概要
システム名: AI品質管理カルテ（大阪センター）

開発言語 / フレームワーク: Python, Streamlit

データベース: Supabase（PostgreSQL）。※ 画像のStorage保存は廃止済み（ファイルは一切保存しない）。

個人情報の取り扱い: 患者名（patient_name）はAIにも読み取らせず、保存・表示・出力のいずれも行わない。レコードの識別子は「伝票番号（slip_number）」。

AIモデル連携: Google GenAI SDK（gemini-3.7-flash を最優先とし、3.6-flash / 3.5-flash / 3.5-flash-lite / 3-flashへの自動フォールバック機構を搭載）

可視化 / 分析: Plotly Express, HTMLレポート生成

2. 機能仕様詳細
🔐 認証・セキュリティ
簡易ログイン認証: Streamlitの Secrets（[passwords] セクション）に定義されたID・パスワードによるゲートキーパー制御。未認証時はDB取得およびAI呼び出しを行わず処理を完全停止（st.stop()）。

セッション管理: ログイン中のユーザー名表示および画面右上からのログアウト機能。

📄 Tab 1: AI一括登録
マルチフォーマット対応: 画像（JPEG / PNG）およびPDFファイルの複数同時アップロード・一括解析（5件単位のバッチ処理）。

スマート名寄せ（AI側）: DBに登録済みの医院名一覧を自動抽出し、プロンプトに注入。手書きの略称や表記揺れを正規名称へ自動補正（判断がつかない場合は原文維持）。

データ検証 & 一括編集: 保存前にプレビュー確認、チェックボックスによる複数行の一括置換（シート種別・種別・材料）、テーブル直接編集が可能。

保存処理: Supabaseへの一括INSERT（文字データのみ）。アップロードしたファイルはブラウザ/メモリ上でのプレビュー確認とAI解析にのみ使用し、保存はしない。

✍️ Tab 2: 手動登録
単票入力: 単一データの登録フォーム（画像添付なし）。

📊 Tab 3: 分析ダッシュボード
連動絞り込み（カスケードフィルター）: 医院・シート種別・期間・材料・種別の条件を選択すると、他方のドロップダウンから0件の選択肢を動的に除外。

品質指標可視化: 適正値（3.0）に対する適正率（%）と平均誤差のリアルタイム算出。

品質偏差アラート & クロス集計: 医院 × 材料別のスコア差（バイト・コンタクトの過不足）を自動検知してアラート表示。

AI専門分析: 歯科技工士の専門視点に基づく傾向と対策のテキスト生成。

医院向けフィードバックレポート: 印刷用スタイルを適用したHTML形式のレポート出力（ダウンロード機能）。

📋 Tab 4: 履歴・管理
検索 & ダウンロード: 伝票番号・医院名のリアルタイム検索（PC/スマホで水平配置された検索バー）および全件CSVエクスポート。

一括操作 & 安全ガード: チェックボックス選択による一括更新および確認モーダル（ポップアップ風UI）付きの一括削除。

医院名統合（名寄せツール）: 過去データの表記揺れ（例:「田中歯科」と「田中歯科医院」）をDB上で一括置換・名寄せする管理機能。

メンテナンスタスク: CSVバックアップからのデータ一括復元（旧CSVに患者名・画像URL列が含まれていても、それらは復元されない）。
3. 設定ファイル仕様 (.streamlit/secrets.toml)
Ini, TOML

GEMINI_API_KEY = "your-gemini-api-key"SUPABASE_URL = "https://your-project.supabase.co"SUPABASE_KEY = "your-supabase-anon-key"[passwords]admin = "labo2026pass"staff = "osaka_dental"